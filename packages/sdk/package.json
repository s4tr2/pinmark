{
  "name": "pinmark",
  "private": true,
  "version": "0.0.0-draft",
  "description": "DRAFT, unpublished. npm-install alternative to the <script> snippet, for JS-framework projects only; the script tag stays required everywhere else. See README.md.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
